import React from 'react';
import Layout from './layout';
import { useData } from '../hooks';

export default ({ location }) => {
  const content = 'PROTECTED FOUNDATION CONTENT HERE';
  return content;
};
