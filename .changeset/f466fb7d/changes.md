- Put `defaultMarks` behind a flag as this feature is still `stage-0`

You may enable `defaultMarks` under the `allowTables` option.

Example:
```
<Editor allowTables={{ UNSAFE_allowDefaultMarks: true }}>
```
