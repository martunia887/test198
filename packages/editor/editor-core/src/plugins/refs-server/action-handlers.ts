import { RefsServerPluginState } from './pm-plugins/main';
// import { ReferenceProvider } from '../refs/provider';

// provider: ReferenceProvider
export const handleAddPushRef = (provider: any) => (
  pluginState: RefsServerPluginState,
) => {
  return pluginState;
};
