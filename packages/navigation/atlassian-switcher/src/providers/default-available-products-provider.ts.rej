diff a/packages/navigation/atlassian-switcher/src/providers/default-available-products-provider.ts b/packages/navigation/atlassian-switcher/src/providers/default-available-products-provider.ts	(rejected hunks)
@@ -11 +11,2 @@ export const createAvailableProductsProvider = (
-    return !result ||
+    return (
+      !result ||
@@ -15,2 +16,4 @@ export const createAvailableProductsProvider = (
-        (site: AvailableSite) => !site.availableProducts || site.availableProducts.length == 0
-      );
+        (site: AvailableSite) =>
+          !site.availableProducts || site.availableProducts.length == 0,
+      )
+    );
