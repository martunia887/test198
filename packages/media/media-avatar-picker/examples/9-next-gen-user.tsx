import * as React from 'react';
import { AvatarPickerExample } from '../example-helpers/stateful-avatar-picker-example';

export default () => (
  <AvatarPickerExample mode="user" containerWidth={200} containerHeight={200} />
);
